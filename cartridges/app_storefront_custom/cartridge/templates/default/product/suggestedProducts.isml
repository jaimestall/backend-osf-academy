<isloop items="${pdict.suggestedProducts}" var="suggestedProduct" begin="${0}" end="${3}">
    <div class="col-md-3">
        <isinclude url="${URLUtils.url('Tile-Show', 'pid', suggestedProduct.ID, 'pview', 'tile', 'ratings', true, 'swatches', true)}"/>
    </div>
</isloop>